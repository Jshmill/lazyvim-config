# LazyVim Config

Config forked off of for [LazyVim](https://github.com/LazyVim/LazyVim).

## Installation

1. Install [LazyVim](https://github.com/LazyVim/LazyVim)
2. Clone this repo into `~/.config/nvim`
3. Run `:Lazy sync` in Neovim to install plugins

```bash
git clone
```
